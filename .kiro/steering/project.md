---
inclusion: always
---

# Projekt MMMRED

Toto je hlavní projektová složka aplikace: `~/Documents/WEB/mmmred`. Next.js app je v podsložce `app/`.

- nikdy nepracuj s `~/kiro-telegram-bridge/`
- Veškerá práce na aplikaci probíhá zde, NE ve složce `kiro-telegram-bridge`.
- Celá aplikace je v angličtině.
- Skilly a agent jsou projektové (`.kiro/skills/`, `.kiro/agents/app-builder.json`): ponytail, improve, design-system-netflix.
- Nové podprojekty/aplikace zakládej dle globálního pravidla pod `~/Documents/WEB/<nazev>` (web) nebo `~/Documents/App/<nazev>` (app).
